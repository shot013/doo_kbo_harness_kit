# MEMORY.md

코드나 git 히스토리만으로는 알 수 없는 프로젝트 맥락(결정, 이유, 진행 상황)을
기록하는 문서입니다. 새 세션(팀원 또는 Claude Code)이 이 파일을 읽으면
"왜 지금 이 상태인지"를 빠르게 파악할 수 있어야 합니다.

작성 규칙:
- 시간순이 아니라 주제별로 정리합니다. 오래된/무효화된 항목은 삭제하거나 수정합니다.
- "무엇을 했다"가 아니라 "왜 그렇게 했다/하기로 했다"를 남깁니다.
- 코드를 읽으면 알 수 있는 내용(구조, 파일 위치)은 적지 않습니다 — `docs/ARCHITECTURE.md`, `CLAUDE.md` 참고.

## 결정 사항

### 경기 일정 스크래핑: Playwright 대신 직접 fetch (2026-08-17)

- **결정**: KBO `Schedule.aspx` 스크래핑을 Playwright 헤드리스 브라우저 대신, 그 페이지가 내부적으로
  호출하는 `/ws/Schedule.asmx/GetScheduleList` AJAX 엔드포인트를 `fetch`로 직접 POST하는 방식으로 전환.
- **이유**: 실제로 Playwright로 네트워크 요청을 캡처해보니 세션 쿠키나 인증 없이도 plain `fetch`로
  동일한 JSON을 받을 수 있었다. 브라우저 프로세스를 띄우는 것 자체가 불필요한 무게였고,
  1GB RAM EC2 인스턴스에서는 이 스크래퍼 때문에 스왑 파일까지 필요했었다.
- **패턴**: 사이트가 내부적으로 XHR/AJAX로 데이터를 받아오는 구조라면, 헤드리스 브라우저를 먼저
  선택하지 말고 그 요청을 네트워크 탭/Playwright로 한 번 캡처해서 인증 없이 직접 재현 가능한지부터
  확인한다. 가능하면 브라우저 자동화 없이 훨씬 가볍게 갈 수 있다.
- **유지한 것**: 진행 중인 경기는 KBO 페이지가 스코어를 0-0으로만 보여주는 한계가 있어, 그 부분만
  네이버 스포츠 API로 보정하는 로직은 그대로 남겼다 (전체를 네이버로 대체하지 않음).
- **부수 효과**: 프로젝트 전체에서 Playwright 사용처가 이 스크래퍼 하나뿐이었던 게 확인되어, npm
  의존성과 Docker 런타임 베이스 이미지(`mcr.microsoft.com/playwright:*` → `node:20-slim`)도 함께
  걷어냈다. 의존성 하나를 없앨 때는 "이걸 쓰는 다른 곳이 정말 없는지" 프로젝트 전체를 grep해서
  확인하고, 있다면 인프라(Dockerfile, CI 이미지 등) 쪽도 같이 정리 대상인지 점검한다.

## 진행 중 / 예정

_(아직 없음 — 진행 중인 기능, 보류된 작업, 다음 단계 등을 기록)_

## 알아두면 좋은 배경

_(아직 없음 — 백엔드 연동 일정, 외부 제약, 팀 합의 사항 등)_
