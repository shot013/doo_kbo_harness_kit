# 기여 가이드

{{TEAM_SIZE_CONTEXT}}이(가) 함께 작업하는 프로젝트이므로, 아래 규칙은 리뷰 비용을 줄이고
충돌을 예방하기 위한 최소 합의입니다.

## 브랜치 전략

- `main` — 항상 배포 가능한 상태. 직접 push 금지, PR로만 병합.
- `develop` — 다음 배포를 준비하는 통합 브랜치. 기능 브랜치는 여기서 분기.
- `feature/{issue번호}-{짧은-설명}` — 새 기능. 예: `feature/42-team-standings`
- `fix/{issue번호}-{짧은-설명}` — 버그 수정
- `chore/{짧은-설명}` — 빌드/설정/의존성 등 코드 동작에 영향 없는 변경
- `release/{version}` — 배포 준비 (버전 고정, 최종 QA)

기능 브랜치는 완료 후 삭제합니다. `develop`이 오래 밀리지 않도록 주기적으로
`main`에 병합하고 새로 분기하세요.

## 커밋 메시지 (Conventional Commits)

```
<type>(<scope>): <subject>

<body (선택)>
```

- `type`: `feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`, `perf`
- `scope`: 영향받는 feature/모듈명 (예: `example`, `router`, `core`)
- `subject`: 명령형, 소문자 시작, 마침표 없이

예시:
```
feat(example): add team standings screen
fix(network): retry request on timeout
chore: bump dependency to latest
```

## PR 규칙

- PR 하나 = 리뷰 가능한 단위 하나. 여러 기능을 한 PR에 묶지 않습니다.
- PR 설명에는 변경 이유와 테스트 방법을 반드시 포함합니다 (템플릿 참고).
- 최소 1명 이상의 승인 후 병합합니다.
- 병합 전 `{{LINT_CMD}}`와 `{{TEST_CMD}}`가 로컬에서 통과해야 합니다.
  (CI에서도 동일하게 실행됩니다.)
- Squash merge를 기본으로 사용합니다.

## 코드 작성 규칙

- 새 기능은 템플릿 모듈의 폴더 구조를 그대로 복사해서 시작하세요.
  (레이어 분리 등 자세한 내용은 [ARCHITECTURE.md](docs/ARCHITECTURE.md) 참고)
- 도메인/핵심 로직 레이어는 프레임워크나 인프라 레이어를 import하지 않습니다.
- `{{LINT_CMD}}`에서 경고가 나오지 않는 것을 기본 목표로 합니다.

## 정책(규칙) 동기화

이 저장소의 `CLAUDE.md`/`.claude/rules/*`/`CONTRIBUTING.md`는 doo_kbo_harness_kit에서
가져온 템플릿을 각 프로젝트에 맞게 손으로 다시 쓴 것입니다 — 여러 저장소가 harness_kit을
공유하지만, 자동으로 동기화되지는 않습니다(의도적인 설계: 저장소마다 팀 규모, 레이어 이름,
언어별 린트 규칙처럼 실제로 달라야 하는 내용이 섞여 있어서 전체 파일을 기계적으로 동기화하면
오히려 오탐만 늘어납니다).

- 규칙이 저장소 간에 벌어졌다고 의심되면(예: 스킬 이름, 브랜치 전략 문구 차이), harness_kit의
  최신 내용과 사람이 직접 diff해서 확인합니다. 자동 sync 파이프라인(CI가 저장소 간에 PR을
  강제로 열거나 덮어쓰는 방식)은 만들지 않습니다.
- 여러 명이 동시에 여러 저장소의 정책을 harness_kit 최신 버전에 맞게 갱신해야 한다면,
  이슈 트래커(Jira 등)에 티켓을 만들고 저장소/파일 단위로 겹치지 않게 1/n로 나눠서
  각자(또는 Claude에게) 맡깁니다. 결과는 항상 일반 PR 리뷰로 검증합니다.
